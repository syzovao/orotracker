{% if (resource_granted('oro_user_user_view') and record.getValue('reporterId')) %}
    <a href="{{ path('oro_user_view', {id: record.getValue('reporterId')}) }}">{{ value }}</a>
{% else %}
    {{ value }}
{% endif %}
