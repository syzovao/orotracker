{% import 'OroUIBundle::macros.html.twig' as UI %}

{% if resource_granted('oro_issue_create') %}
    {{ UI.clientButton({
        'dataUrl': path('oro_issue_create', { 'userId': entity.id }) ,
        'aCss': 'no-hash',
        'label': 'oro.issue.create_issue'|trans,
        'widget': {
            'type': 'dialog',
            'multiple': false,
            'reload-grid-name': 'user-issues-grid',
            'options': {
                'dialogOptions': {
                    'title': 'oro.issue.create_issue'|trans,
                    'allowMaximize': true,
                    'allowMinimize': true,
                    'dblclick': 'maximize',
                    'maximizedHeightDecreaseBy': 'minimize-bar',
                    'width': 1000
                }
            }
        },
    }) }}
{% endif %}
